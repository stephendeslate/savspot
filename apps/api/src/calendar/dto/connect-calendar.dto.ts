/**
 * DTO for initiating a Google Calendar OAuth connection.
 * The body is empty — OAuth is initiated server-side.
 * This class exists for Swagger documentation and validation pipeline consistency.
 */
export class ConnectCalendarDto {
  // No fields — OAuth redirect is generated entirely server-side
}
