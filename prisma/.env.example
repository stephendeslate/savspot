DATABASE_URL="postgresql://savspot:savspot_dev@localhost:5432/savspot_dev?schema=public"

# DIRECT_URL is used by Prisma Migrate (CLI) and any code path that needs a
# session-mode connection. In hosted Supabase deployments, set DATABASE_URL to
# the Supavisor pooler (port 6543) and DIRECT_URL to the direct DB host (5432).
# Locally, leave DIRECT_URL unset — Prisma falls back to DATABASE_URL.
# DIRECT_URL="postgresql://postgres:<password>@db.<ref>.supabase.co:5432/postgres?sslmode=require"
